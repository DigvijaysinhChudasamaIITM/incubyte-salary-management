[project]
name = "salary-management-deployment"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["salary-management-api"]

[tool.uv]
package = false

[tool.uv.sources]
salary-management-api = { path = "./backend" }
